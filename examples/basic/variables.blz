// Variables, constants, booleans, and null.
var language = "BlazeLang"
constant VERSION = "2.0"
var enabled = true
var optionalValue = null

Show(language + " " + VERSION)
Show(enabled)
Show(optionalValue)
